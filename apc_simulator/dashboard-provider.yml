apiVersion: 1
providers:
# <string> an unique provider name. Required
- name: 'Demo Dashboard Provider'
  folder: Project Demo
  type: file
  allowUiUpdates: false
  options:
    # <string, required> path to dashboard files on disk.
    path: /var/lib/grafana/dashboards/project_demo
